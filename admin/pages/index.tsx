import React from 'react';
import Logo from '../../shared/Logo';

export default function Dashboard() {
  return (
    <div style={{ background: '#0B0B0B', color: 'white', minHeight: '100vh', padding: '2rem' }}>
      <Logo />
      <h1>Dashboard</h1>
    </div>
  );
}
